name: Update Players Data

on:
  schedule:
    # Tuesday at 5AM EST (10AM UTC)
    - cron: '0 10 * * 2'
    # Thursday at 5AM EST (10AM UTC)
    - cron: '0 10 * * 4'
    # Sunday at 10AM EST (3PM UTC)
    - cron: '0 15 * * 0'
  workflow_dispatch: {}

# Needed to push updated CSVs back to the repo
permissions:
  contents: write

concurrency:
  group: players-scheduled
  cancel-in-progress: false

jobs:
  ingest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Ensure dependency manifest exists
        run: |
          if [ ! -f requirements.txt ]; then
            echo "pandas" > requirements.txt
            echo "requests" >> requirements.txt
          fi

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Create .ENV from secrets/variables
        run: |
          cat > .ENV <<'EOF'
          mfl_api_key='${{ secrets.MFL_API_KEY }}'
          mfl_league_id='${{ secrets.MFL_LEAGUE_ID }}'
          current_season='${{ vars.CURRENT_SEASON }}'
          EOF

      - name: Run players ingestion (current season only)
        run: python ingest/players_current_season.py

      - name: Remove ephemeral .ENV
        run: rm -f .ENV

      - name: Commit and push changes (if any)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          # Rebase on latest to minimize push conflicts
          git pull --rebase || true
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "chore(data): update players (current season) on $(date -u +'%Y-%m-%d') (run ${{ github.run_id }})"
            git push
          fi

